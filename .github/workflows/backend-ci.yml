name: DIAM-YARAAM Backend CI

on:
  push:
    branches:
      - develop
      - main
      - 'feature/**'
      - 'release/**'

  pull_request:
    branches:
      - develop
      - main
      - 'release/**'

  workflow_dispatch:

permissions:
  contents: read

jobs:
  backend-ci:
    name: Build & Tests Backend
    runs-on: ubuntu-latest

    steps:
      # 1. Récupérer le code
      - name: Checkout du repository
        uses: actions/checkout@v4

      # 2. Installer Java 21
      - name: Installer Java 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'
          cache: maven

      # 3. Installer Maven 3.9.6
      #    On utilise la même version que celle de ton Maven Wrapper.
      - name: Installer Maven 3.9.6
        shell: bash
        run: |
          curl -fsSL \
            https://repo.maven.apache.org/maven2/org/apache/maven/apache-maven/3.9.6/apache-maven-3.9.6-bin.tar.gz \
            -o "$RUNNER_TEMP/maven.tar.gz"

          tar -xzf "$RUNNER_TEMP/maven.tar.gz" -C "$RUNNER_TEMP"

          echo "$RUNNER_TEMP/apache-maven-3.9.6/bin" >> "$GITHUB_PATH"

      # 4. Vérifier les versions
      - name: Vérifier Java et Maven
        run: |
          java -version
          mvn -version

      # 5. Compiler + exécuter les tests
      - name: Build et tests
        run: |
          mvn --batch-mode --no-transfer-progress clean verify